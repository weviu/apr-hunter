# API base URL (defaults to same-origin /api when left empty)
# Set only if your frontend must call a separate host.
NEXT_PUBLIC_API_URL=

# WalletConnect Project ID (set to enable wallet integrations)
# Leave empty to disable WalletConnect locally.
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

